# AI-TOD ablation, row 5 of 6: row 4 plus the null entry of every cross-attention head (switch 5
# of include/ours.yml). This is every switch of ours.yml at the baseline's fixed 300 queries,
# i.e. DFine-S-AITOD-Ours.yml without the query budget: row 6 minus this row is the budget's
# own value.

__include__: ['./DFine-S-AITOD-4-enc-quality.yml']

output_dir: ./outputs/ablation/aitod_s_5_null_point

DFINETransformer:
  null_point: True
