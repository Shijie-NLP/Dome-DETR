# AI-TOD ablation, row 4 of 6: row 3 plus the encoder's quality target from the decoder's final
# box (switch 3 of include/ours.yml).

__include__: ['./DFine-S-AITOD-3-min-cells.yml']

output_dir: ./outputs/ablation/aitod_s_4_enc_quality

DomeCriterion:
  enc_quality_source: final
  dec_quality_source: own
