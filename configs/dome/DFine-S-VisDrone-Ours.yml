# D-FINE-S on VisDrone2019-DET with our method switched on: DFine-S-VisDrone as it is, plus the
# switches of include/ours.yml (the fine level, the sampling floor, the outcome-scored encoder,
# cell anchors, the null entry). An ablation is this file with one of them reset.

__include__: [
  './DFine-S-VisDrone.yml',
  './include/ours.yml',
]

output_dir: ./outputs/dfine_s_visdrone_ours
