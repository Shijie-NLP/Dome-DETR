# D-FINE-L on AI-TOD-v2 with our method switched on: DFine-S-AITOD-Ours at the L scale, i.e.
# DFine-L-AITOD plus include/ours.yml and the per-image query budget (the buckets, budgets and
# grouped batches are explained in DFine-S-AITOD-Ours.yml). The B4 stem map is 64 channels wide.

__include__: [
  './DFine-L-AITOD.yml',
  './include/ours.yml',
]

output_dir: ./outputs/dfine_l_aitod_ours

HybridEncoder:
  fine_in_channels: 64

DFINETransformer:
  query_budget: bucket
  count_edges: [100, 200, 300, 600]
  count_budgets: [300, 400, 500, 800, 1500]
  count_quantile: 0.9
  count_train_budget: gt

train_dataloader:
  group_by_count: {edges: [100, 200, 300, 600]}
